spring:
  application:
    name: backend
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: validate

springdoc:
  api-docs:
    enabled: false

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-millis: ${JWT_EXPIRATION_MILLIS:3600000}
    refresh-expiration-millis: ${JWT_REFRESH_EXPIRATION_MILLIS:1209600000}
    refresh-cookie:
      name: refreshToken
      secure: ${IS_COOKIE_SECURE:true}
      same-site: ${SAME_SITE:Lax}